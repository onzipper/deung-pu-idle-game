# Claude status log (historical)

Superseded per-round status blocks moved verbatim out of `CLAUDE.md` (issue #45).
Newest first. Each `##` heading was one "Earlier (vN)" segment of the original
single status paragraph — content is unmodified. The LIVE status lives in
[`docs/current-state.md`](../current-state.md); when a round closes, the
superseded block from there gets appended here.

## Status v18 (2026-07-10, R4.5 Wave 2 Map2/Greenmill path — SUPERSEDED by owner direction reset)

Superseded block from `docs/current-state.md`, moved here when the owner reset the map/field direction to **World Arc v1 + Free-field 2.5D** (`docs/world-arc-freefield-v1.md`) later the same day: **Arc position read "R4.5 map direction IN PROGRESS (#69 — Phase 0 approved, Waves 1+1.1+1.2 merged; owner passed Wave 1 depth/scale/contact shadow; Wave 2 map design NOT accepted yet, see #79)". Owner eye-test verdict: R4.5 Wave 2 `map2` farm-zone map design NOT accepted; the stacked Draft PRs #73 (2A spec v2 Greenmill Hamlet/Farm Border Road) → #74 (2B ground) → #75 (2C props, merged INTO the 2B stack branch only) → #76 (2D polish) remained Draft/prototype pending a map reference in #79. Blocker read: "do not merge Wave 2 stack as final visual direction until a map reference/design is approved (#79)". Owner-decision row: "R4.5 Wave 2 map2 farm-zone design rejected for now; use #79 to lock map reference before accepting/merging Wave 2 visuals". Next-work items: create/choose a map2 reference (#79) then revise the Wave 2 stack against it; keep #73→#76 Draft. RESOLUTION: none of this happened — the direction reset retired the x-axis-first field model and the Map2/Greenmill design entirely; #73/#74/#76 to be commented/closed as superseded, uncommitted 2B WIP preserved in a labeled stash on `r45/issue-69-wave2b-ground`. Zone-progression mapping (a) (owner-confirmed on PR #73 that morning) is likewise superseded — recorded in `docs/decision-index.md` as a superseded row. Wave 1 depth language + Wave 1.1/1.2 + R4 x/y plumbing all carry forward into the new direction.**

## Status v17 (2026-07-10, R3)

Status (2026-07-10 v17, R3 presence คนจริง, issue #50, branch `r3/issue-50-presence-actions`, **Draft PR #62** staged by wave): **W1-2 relay additive `pa` (fan≤12, 256B, 100ms/conn rate guard, NOT cached, never liveness — `p` stays sole snapshot-on-join+prune source; +8 relay tests, protocol doc §12). W4 receive/render: ghostStore GhostAction (facing/action/actionAt/lastActionAtMs, `lerpAt` SPLIT from `lastAt` so pa re-anchors lerp without extending life; stale-at reject + reset rule 10s-silence-or-backward-1000; pa never spawns ghosts), worldSession onGhostAction+publishAction, ghostLayer GhostPose render-only (NO GameEvents, edge-trigger once per advancing at, p-only ghosts pixel-identical), heroView additive-only `playHeroPosePulse` (arm pose, zero fx/camera/audio path). W3 publisher: buildActionSample/shouldPublishAction pure, PRESENCE_ACTION_BEAT_MS=125, `a` derived basic=cd-upward-reset edge / skill1-4=skillCast→autoSlots / dash=heroDashed, `at` bumps real edges only, edges LATCHED EVERY rAF FRAME (between-beats drop trap), valve `actionBeatMsForEma` 8→4→0Hz sharing GHOST_VALVE_HEAVY/LIGHT_MS (33/22) with ghost cap 12→6→0 — single source, can't drift. W5 tap profile: GameRenderer.hitTestGhost (sibling of hitTestNpc, reads renderer ghostList never engine), pointer order npc→monster→gate→ghost→ground, ghost tap = ZERO pendingInput writes (reference-equality test), GhostProfileCard ModalPortal view-only, gameStore ghostProfileCid session-only. Patch notes 2026-07-10e. Invariants: presence=render/store-only, farm mobs private, party lockstep untouched (separate socket/state), engine combat authority unchanged. W6 ghostGuard expansion (2×800 turns + injected pa + taps ⇒ hash-equal) + W7 docs (ghost-presence-design/world.md/ROADMAP R3 checklist/feature-map) landed same branch. OWED: owner review of Draft PR #62 → ready-for-review → merge confirm → **deploy RELAY FIRST then web, no db push** → 2-tab manual test (snapshot fast, ~8Hz alive, tap profile no-command, relay-down silent degrade).**

## Status v16 (2026-07-10, R2.9)

Status (2026-07-10 v16, R2.9 Codegen Asset Phase 1A, issue #60 branch `ui/issue-60-codegen-icons-1a` PR→develop): **#57 Asset Bible closed the loop: owner attached GAME ASSET OVERVIEW sheet + decisions (HD-pixel hybrid · generated=concept-first · item icons first · PNG later NOT NOW — corrected direction: code-drawn only, no image files/loader/atlas; PNG Phase 1 deferred indefinitely; character/monster sprites hard-gated on R4-R5; damage-number art direction → future VFX). #60 = 9-id vertical slice: NEW `src/ui/components/icons/` (iconBase useId-gradient seam / itemIcons / skillIcons / gameIcons contract file) — filled-silhouette + gradient + family-glow SVG language replacing gold-line+emoji for items/skills; registry id→component; `ItemIcon`/`SkillIcon` render registry SVG or the labels.ts glyph fallback VERBATIM (ids outside slice byte-identical). Items: w_sword_t1_rusty, w_bow_t1_short, w_sword_t10_apocalypse (epic gold glow), a_cloth_t1_tunic, fort_weapon (deliberately exercises lookupTemplate superset trap). Skills: sword_whirl, mage_meteor, mage_frostnova, archer_rain. Consumers wired via the one seam: ItemTile (+optional `templateId` prop — call sites InventoryPanel/EquipmentDoll/RefinePanel pass it), SkillBar, SkillDetailModal, BotSettingsModal picker; cooldown/badges/rarity frames untouched; botSettingsSkillPicker tests needed zero changes. Tests: iconRegistry (9: key-resolves-via-lookupTemplate + SKILLS existence + fallback verbatim + crash-free) + ItemTile (3). Constraints held: no <filter>, ≤~25 paths/icon, useId gradient ids, no image files in diff (grep-asserted). Patch notes 2026-07-10d. OWED: owner eye-test the slice (inventory/refine/dock/detail/BOT, mobile+desktop, bright+dark biomes — two icon languages coexist until Phase 1B by design) → Phase 1B = remaining ids after approve.**

## Status v15 (2026-07-10, R2.8)

Status (2026-07-10 v15, R2.8 Wave B safe items, issue #58 branch `ui/issue-58-wave-b` PR→develop): **Wave A merged same-day (PR #56 → develop, review-fix commit b0a37fd added CurrencyChip `danger` variant + typed cost aria + copy typos per owner review). #54 got the Wave B split proposal: B1 safe (no decisions) vs B2 gated on owner Qs (menu-row scope / action rail / chat), asset topics → #57 Asset Bible incl. damage-number art direction. Owner opened #58 = B1: (1) BOT auto-skill picker rebuilt from vertical checklist → 64px icon-tile row mirroring SkillBar tile language (numbered ordinal badge, emerald ✓ = on per in-modal convention; `setAutoSlot` semantics untouched, no engine fields); (2) mobile HUD tuning CSS-only — menu-row mobile 3→2 rows (`grid-cols-5` + IconTileButton 40px below `sm:`, documented exception to the 44px game-ux rule), SkillDock py/gap tightened below `sm:`, quest-slot 78vw→72vw + doc comment proving portrait↔tracker can't structurally overlap, FTUE anchors all unchanged; (3) REAL BUG found by the tuning agent: `ExpClockStrip`'s box was only as tall as its 4px bar while its absolute labels rendered above the box top — text collided with the dock gap on every viewport (worst on short landscape); fixed with `pt-3.5`. Patch notes 2026-07-10c (botSkillRow/hudMobileTune/expStripFix th+en). New test file botSettingsSkillPicker (4 tests, CODEMAP synced). Suite 2291+ green, lint/tsc/build clean (one transient build fail from concurrent json edit mid-build — clean on rerun). OWED: owner eye-test #58 checklist (BOT picker tap feel, mobile portrait 2-row menu, short-landscape bottom stack) + PR merge confirm; B2 still gated on #54 owner questions.**

## Status v14 (2026-07-10)

Status (2026-07-10 v14, R2.7 Wave A UI polish, issue #55 branch `ui/issue-55-wave-a` PR→develop): **#54 UI design-audit ran first (discussion only, no code): 2 audit agents (gameplay HUD / feature panels) + 2 feasibility passes (render / React-store seams) → 64-row scorecard match 40 / partial 18 / missing 6 (all missing = locked-defer or backlog), posted to issue #54 with waves A–D + owner questions. Key audit facts: engine has NO crit mechanic (ref's "Crit!" juice is engine+owner scope; render side 100% ready); menu-row grew to 10 buttons vs ref ~7 (owner decision pending); BOT confine-to-zone needs an engine `BotSettings` zone field (none exists); inventory ใช้/อื่นๆ tabs blocked on economy (potions/materials are counters, not items); R3 ghost crowd fx needs a shared cheap visual + cap or mobile 60fps breaks. Owner opened #55 = Wave A implementation (6 items, web only): EXP % readout on ExpClockStrip (left edge, gold numeral) · damage/gold floating-number black stroke (first TextStyle.stroke in repo — set once at Text construction, per-spawn mutation untouched = zero per-spawn cost) · top-edge gradient scrim at z-5 beside the vignette in GameHud (from-black/35, z-ladder doc updated) · RefinePanel cost chips now owned/required fractions (42/30 style) with red tint via className override when short (CurrencyChip stays presentational; reveal-on-final-strike untouched) · Toast info variant → violet chrome (border-violet-400/40 bg-violet-950/85, text stays ink) · InventoryPanel "ทั้งหมด" tab (activeTab widened GearSlot|"all", DEFAULT all; equipped-compare derives from selected item's own slot; EquipmentDoll untouched, gets derived fallback). 3 parallel agents in disjoint file zones + i18n patch-notes agent. New i18n keys: refine.costAria, inventory.slot.all (th+en parity verified). 3 new test files (floatingText stroke, inventoryAllTab, refinePanelCostChips) + gameHudLayout Wave A block; CODEMAP synced. OWED: owner eye-test Wave A (bright-biome readability, 42/30 chips, all-tab default) + PR review/merge to develop; Wave B awaits owner answers on #54 questions (crit / menu-row scope / action rail / chat).**

## Status v13 (2026-07-09)

Status (2026-07-09 v13, M8.10 R2.5 Game Screen + input-drop root fix): **Owner played R2 → "ไม่เห็นตรง ref เลย" → diagnosis: R2 reskinned PANELS but the GAMEPLAY SCREEN kept the old framed 3:1 structure while the ref's essence = fullscreen canvas + all-overlay HUD → R2.5 shipped on develop (5 commits after ea26997, ROADMAP M8.10): W0 INPUT-DROP ROOT FIX — solo frame path drained pendingInput BEFORE computing accumulator steps; 0-step frames (90Hz+ displays, ~guaranteed first rAF after boot) silently discarded intents = the owner's "bot won't default OFF on localhost" bug + occasionally swallowed taps; fix = pure drainSoloFrame (steps first, drain only when >0), cohort path audited zero-loss, exactly-once tests — THE bug-class to remember: one-shot intents through the rAF accumulator MUST drain only on stepping frames. W1 fullscreen render (computeFullscreenTransform BAND_BIAS 0.58 + SKY/GROUND/X bleeds, ==old transform at exactly 3:1; camera viewW param, posX deliberately stays WORLD_WIDTH/2; filterArea+mask track visible rect camera-ON-only; sky 16 bands, groundBand depth shading, bossArena frames to bleed; setFilterOrigin fixes shockwave clip; +33 tests). W2 all-overlay HUD both platforms (page h-dvh, canvas absolute inset-0, framed layout DELETED; HudBar+WalkControls dissolved; HeroPortraitCard top-left, top-right CurrencyChipsRow + IconTileButton menu row + NEW CharacterPanel absorbing StatPanel/EquippedLoadout/SwitchCharacter; GoalLadder portal ALL viewports w/ mobile compact + FTUE force-expand; skill dock + ExpClockStrip; FTUE re-anchored stat-panel→character-menu settings-row→menu-row; z-ladder documented; RTL smoke + jsdom devDeps). W3 npcTrip replaces smithTrip (pahpu/lungdueng/elder via the ONE TownNpcPanelHost seam; closed smithTrip's missing fastTravel/warp cancel) + NpcTripButtons walk-order-only (owner R2.5 เคาะ SUPERSEDES the older no-HUD-shop rule — recorded in ui-reference-map.md; dim+toast not native disabled) + MiniMapCard (zone name/SVG strip hero-gates-NPCs/👥 via useZoneCounts {open,pollMs} 30s hidden-pause; tap→WorldMapPanel); RefineButton kept unused pending owner confirm. NEW DOCS DISCIPLINE RULE (owner, commit ea26997) applied all round: docs/CODEMAP.md synced on every file add/delete/move, enforced by src/__tests__/codemap.test.ts. Suite 2249/2249, tsc/eslint/next build clean, patch-notes 2026-07-09o. OWED: owner eye-test vs the ref (fullscreen mobile portrait/landscape + desktop, bot-off on his 120Hz localhost NOW works, taps never vanish, NPC walk-order buttons feel, minimap, FTUE full run on a fresh char, boss HP plate new mid-screen position) → merge R2+R2.5 to main as one block on his confirm → deploy relay-first then web, no db push.**

## Earlier (v12, M8.8 MERGED + M8.9 R2 done): **M8.8 R1 MERGED TO MAIN (PR #44, owner pre-confirmed "ฝากเมิจ"; deploy STILL OWED: relay first — counts endpoint — then web, no db push; bot-off-default + gates only visible on prod AFTER deploy — owner reported "ยังไม่ได้" pre-deploy, code path verified + headless-test-pinned). Owner asks post-R1: gate tap now WALKS to the gate first then transitions (gateTrip state machine mirrors smithTrip: moveTo → within 40px → walkToZone once; cancels on other command/NPC talk/death/warp/20s; mutual-cancel w/ smithTrip; HeroSummary.x added ~10Hz) · bot master OFF at every entry (queueSetAutoHunt(false) after offline replay so replay income untouched; store pre-hydration default false; FTUE welcome/watchFight/botSwitchIntro copy now teaches tap-to-move + AUTO opt-in; BEHAVIOR NOTE: ending a session with bot off ⇒ next offline window earns nothing — replay uses saved state, as requested). M8.9 R2 "UI Sweep" DONE on develop (5 waves, suite 2188/2188, tsc/eslint/next build clean, patch-notes 2026-07-09n): W1 primitives set-2 (Tab/TabRow purple-active, CurrencyChip, StatBar, ItemTile off labels.ts, Toast, ConfirmPopup) + inventory grid/shop 3-tab migrated (useConfirmGuard two-tap kept — modal swap = behavior change) · W2 always-on HUD (portrait block w/ class roundel+Lv+power+StatBars mobile-height-neutral; CurrencyChips; numbered skill slots; AUTO moved WalkControls→SkillBar w/ gold invite-pill OFF state; potion quick-slot tiles) · W3 RefinePanel success-% shown (already single-sourced: UI + server both read engine/config/refine.ts) + ATK before→after line, reveal state machine untouched; EquipmentDoll rebuilt (roundel center, flanking ItemTile slots, 4-stat block — NO AGI, engine has 4; mobile scroll strip fixes a pre-existing 375px overflow; HERO_ACCENT promoted to labels.ts) · W4 SkillDetailModal (list+detail off skillStatParts, NO skill-leveling UI — parked backlog), BotSettingsModal regrouped 3 mockup sections + SkillAutoSlotPicker (entry stays the single ⚙ beside AUTO), HOF gold shell + TabRow (podium logic untouched) · W5 desktop arena max-w 3xl→5xl/6xl/7xl + GoalLadder PORTALED onto the arena on md+ (portal not show/hide — FTUE goal-ladder anchor querySelector would grab a hidden dup; mobile DOM byte-identical; true 16:9-tall stage deferred to R4 x,y which must extend the sky render). OWED: owner playtest (gate-walk feel · bot-off default AFTER deploy · all panels on mobile esp. doll strip/RefinePanel/Bot panel · new AUTO position · desktop overlay vs BuffBadgeHub collision) then merge confirm for M8.9. NEXT per arc: R3 presence คนจริง (action stream ~8Hz + snapshot-on-join + tap profile; relay opcode pa additive) → R4-R5 engine x,y.**

## Earlier (v11, VISION v3 + M8.8 R1): **GDD bumped to v3 (owner-เคาะ): open world MMO RPG 2.5D ที่มีบอทเล่นแทน — ไม่ full 3D/ไม่หมุนจอ · เดินแกน x,y จริง = engine 1D→2D milestone R4-R5 · "เข้าโซนเจอคนจริงไม่มีพิธี connect" = โลก 3 ชั้น (rich presence action-stream ~8Hz + snapshot-on-join / shared-entity ledgers ขยายจากเสี่ยจ๋อง / party lockstep เดิม — server-authoritative REJECTED by 2 independent deep-consults; ห้าม copy สัญญา "ตีร่วมกันได้ทุกตัว") · Arc R1✅→R2 UI sweep→R3 presence จริง→R4-R5 x,y→R6 shared elites (ROADMAP "Arc Open World MMO"; ตลาดกลางแทรกตามจังหวะ). UI = Dark Fantasy+Gold+Purple จาก reference ที่เจ้าของให้เอง (art-references-first SATISFIED; เฟสแรก art จากโค้ดล้วน) — full mapping + all เคาะ in docs/ui-reference-map.md (คงกติกา NPC · ตีบวกโชว์ % ที่ R2 · epic คงทอง ม่วง=chrome · no joystick · backlog: เลเวลสกิล gold-sink + คอลเลคชัน · พัก: กิลด์/สัตว์เลี้ยง/เซ็ต/CH). M8.8 R1 "โลกใหม่ หน้าตาใหม่" DONE on develop (plan whimsical-frolicking-naur, commits f298621/e6d73ca/4a4d2bc/8bff353+docs): W1 design system v1 (gold-frame tokens + Kanit 600/800+Prompt 400 แทน Chakra Petch + primitives Panel/PanelHeader/Button + 8 SVG line icons + pilot FastTravelPicker/InventoryPanel shell-swap) · W2 ประตูแตะได้แทนปุ่ม ◀▶ (M7.5 scenery gates → interactive: gateLockOverlay padlock+เกจ/open-glow/boss crownColor, hitTestGate camera-aware via pure gateTapSide, tap order npc→mob→gate→ground, fires the EXACT walkToZone intent = intent-parity test, arrows removed from WalkControls) · W3 relay GET /presence/counts (additive beside /health, joined-only, empty-omitted) + relayUrlCache seam · W4 แผนที่โลก (zone chip→WorldMapPanel: live counts/friend lastZone initials/party dot/🔥/world-boss badge MAP-LEVEL-ONLY — "คนกอง=ใบ้บอส" accepted as hunt aid; poll 10s panel-open only, old relay degrades silent; travel = queueFastTravel same rule as picker; mapTheme.ts extracted shared) · patch-notes 2026-07-09m (3 items th/en). Suite 2123→**2163**, tsc/eslint/next build clean. **DEPLOY M8.8: redeploy RELAY FIRST (counts) → web, NO db push. OWED: owner playtest before merge (โฉมใหม่ both devices/ฟอนต์อ่านง่าย · gate taps near mobs/mid-zoom/boss door · world map 2-tab live counts · relay-down degrade) + M8.7 spot-checks still open (night-after-tune/tap mid-zoom/hp-bar depth scale/mobile fps).**

## Earlier (v10, M8.7 RELEASED to main): **M8.7 โลกมีมิติ MERGED TO MAIN (PR #43 merge 7b1eab4, owner-confirmed) + patch notes 2026-07-09l (7 items th/en — closes the owed release-round notes) + owner-playtest fix round on the same merge (commit 543f311, plan happy-mixing-elephant): (1) NIGHT TOO DARK — actors were double-darkened (entities tint 0x8a97c8 multiply + 0x101c4a overlay @0.35); owner picked "actors readable, backdrop keeps mood" → new `ENTITY_TINT_RELIEF=0.5` knob + pure `entityAmbientTint()` in dayNight.ts (entities container only; background/ghosts keep full ambient; white→white so noon/OFF identity holds) + `OVERLAY_ALPHA_MAX` 0.35→0.26 (the overlay rect also washes fx/damage numbers — tint relief alone can't restore contrast); both are single-constant eye-tune knobs if owner wants more/less. (2) OWN ฉายา HIDDEN ON OWN SCREEN (peers saw it — asymmetry): `mySocialBadge` was only populated by the bot-only `townArrived` event or a Settings re-pick → now `refreshHofOnTownArrival()` also fires once at boot in GameClient. Suite 2120→2123, tsc/eslint clean. DEPLOY OWED: web redeploy ONLY (no db push, no relay). Owner spot-checks owed: night after the tune (mobs/hp bars readable + mood intact; night peaks min ~22.5–28 of each shared 30-min cycle) · own title appears after reload · tap accuracy far/near mid-zoom · depth-scaled hp bars ±20% acceptable? · mobile fps · Pixi v8 stable-sort assumption for OFF z-order.**

## Earlier (v9, M8.7 build round): M8.7 merged to develop (merge 67feff2) — born as /lab exp ⑨ the same morning (owner: "pattern เดิมซ้ำๆ น่าเบื่อ... จะไปแนว open world idle MMO"), then promoted through 7 planned waves. Depth band (mobs+heroes+ghosts; hash/slot-lane d; root pivot.y=GROUND_Y at CREATION SITES ONLY so rig.test stays byte-identical; scale-around-feet; entities+ghosts sortableChildren) · living camera (new cameraRoot holds background/ghosts/entities/projectiles/fx, overlay/boss-bars stay on world; mask GATED on camera-on to preserve OFF-identity; follow 1.06 / idle→1.0 FULL VIEW — inverted vs the lab's idle-zoom-out because a 900px world can't go below 1.0; hitTestPointer/hitTestNpc invert base+camera via pure hitTestMath; impactFilters trigger mapping gained the camera term) · per-zone terrain (deterministic preset via hash(mapId:zoneIdx); town/boss zones + gate-x ±90px EXACT flat; ground polygon + darkened backing strip; groundProps conform via ParallaxLayer.conformY per-frame chunk-center sampling; Environment.setTerrainEnabled busts currentKey → 1s crossfade) · living world (30-min shared day cycle off Date.now — the one sanctioned wall-clock read, in draw; ambient tint on background/ghosts/entities NEVER fx/projectiles so damage numbers stay readable at night; per-zone weather = hash(zoneKey, 20-min window) per-map sets; birds day/fireflies night; density valve rides the ghost fps EMA — only ticks while ghosts feature on, renderer default 1 otherwise). ENGINE TOUCH = exactly ONE additive field: kill event `id` (transient, never hashed; determinism suite byte-identical) — everything else render/ui; 33 FxController ground anchors rewritten as provably-+0-when-OFF deltas via shared worldFxContext (pure-recompute seam, NOT view-reading — kill-time views are pool-released before consumeEvents). Settings → "โลกมีมิติ": worldDepthOn (depth+terrain) / worldCameraOn / worldAtmosphereOn, default ON, localStorage client-pref tier, all-OFF = pixel-identical (identity tests pin it). Modules live in src/render/worldDepth/** (git mv from lab; lab exp ⑨ imports @/render/worldDepth = correct boundary direction). Suite 2004→2120 (+116), tsc/eslint/next build clean. SAME ROUND: level-cap announcement now fires for EVERY capper once-per-character (singletonKey `cap:<charId>` @unique, was the global first-to-cap singleton; copy 'เวลตันแล้ว' th/en; the concurrent double-save race still P2002-swallowed) — comment-only schema, NO db push. DEPLOY M8.7: web redeploy ONLY (no db push, no relay). (Release-round owed items — patch notes + spot-check list — now tracked in the v10 block above; full quirk list in ROADMAP M8.7.) Owner flagged token burn mid-round → frugal orchestration re-committed (1 agent/wave, inline small fixes, single verify pass; memory updated). The v8 parallel-lab-session guardrail is OBSOLETE (that WIP shipped via commit 6c657ed).**

## Earlier (v8, M8.6 live-feedback round 1): **PR #37 (M8.6) merged+deployed, owner played with friends and reported 6 bugs + 3 UX asks — ALL FIXED on develop (plan ghost-woolly-patterson v2, tests 2000/2000, patch-notes 2026-07-09k, sim gates re-verified at the engine change):** (1) bot settings PER-HERO for real (the M8.6 setBotSettings strip had no replacement channel — HeroConfig widened w/ the six BotSettings fields transient/hash-excluded, solo invariant config≡state.bot via syncPrimaryHeroConfig = byte-identical, per-lane apply in step.ts, nextBotSettingsWish latch, bots.ts reads heroes[0].config, serialize/collapse overlay onto SaveData.bot) · (2) per-member unlock TRUTH (owner case: A@z38+B@z31 co-farm z31 → B saw a fake "ปลดสำเร็จ" from A's shared unlockedZones, couldn't walk on — snapshot now derives MY unlockedZones via pure deriveUnlockedZones cascade w/ boss-gate/preview-map conservatism + one-shot unlock notice) · (3) asura per-member accounting (essence = mean-field splitField, asuraZoneKills full-credit, tomePages OR + tomeUnlocked latch, sigils = base + MY drained claims; claimAsuraSigil/craftLegendary were lane-0-only leaks — a member's sigil claim was server-consumed but engine-LOST, now stripped + counted; craft disabled in-party w/ hint; settled fields spread into snapState) · (4) hidden tab = LANE-KEEPALIVE (folded member stays ACTIVE: ws stays open, CohortTurnEngine.issueOnly keeps emitting idle lanes driven by inbound peer messages + 1s interval — peers never stall, hero keeps farming; resume = budgeted burstExecute backlog bypass w/ hash-parity test; >30min/ws-died falls back to legacy collapse+catchUp; TRUE close = pagehide/freeze clean-close instant shadow; relay heartbeatMs 15000→5000 = silent-death stall ~35s→~10-12s) · (5) world-boss banner knows DEFEATED (deriveWorldBossStatus 4-arg, all-clients 12s state poll during the window, window-scoped participation/claim latches, emerald "ล้มแล้ว" state, AUTO-CLAIM for participants who left the zone) · (6) signal-chip 0ms → "✓ ตามทัน" (perSlotLag clamps the healthy −2 lookahead to 0; display-only formatMemberLag). UX: refine ตีบวก button works from ANYWHERE (smith-trip state machine ui/world/smithTrip.ts + SmithTripWatcher: fastTravel→walk to ลุงดึ๋ง→auto-open panel, cancel on tap/death/block) · warp button moved beside the top-left zone chip (removed from WalkControls — one-place rule) + player-facing "ด่าน"→"โซน" (HUD label + picker range headers only) · refine button restyled to the InventoryButton idiom. **DEPLOY: redeploy RELAY (heartbeat 5s; presence/chat/ping if not yet live) + web; NO db push. Owed: in-browser playtest of smith trip + mobile warp placement + 2-tab keepalive/bot-settings/unlock-truth pass; owner confirm before merge.** NOTE: a PARALLEL owner session works /lab (pixelWeaponFx/refineFxRecipes/heroView legendary-flourish WIP uncommitted + lab-assets churn) — never git-add src/lab/**, src/render/views/heroView.ts, public/lab-assets while it runs; filter its tsc errors out of verification.

## Earlier (v7, world-layer round): **owner confirmed party lag GONE post-deploy ("ไม่หน่วงแล้ว ดีมากๆ") → M8.6 World Layer SHIPPED on develop (plan ghost-woolly-patterson, design docs/ghost-presence-design.md v2, ROADMAP M8.6): ghost presence + world chat + party UX + cohort live-bug wave — tests 1938/1938, tsc/eslint/next build clean, patch-notes 2026-07-09j.** (1) **Cohort live-bug wave (babc942, all owner-reported):** town zones NEVER form lockstep cohorts (`deriveCohort` returns [mySlot] on zoneAt kind "town" — kills the town bot-potion-trip deadlock; party membership/panel untouched; engine cohort-town branches kept as defense-in-depth) · position survives collapse/re-form (`extractSoloState` passes a positions map + `beginHandshake` sends myX UNCONDITIONALLY — the unshadow/reconnect paths collapse first so the old cohortActive gate always lost x) · bot master toggle works mid-cohort via pure `nextAutoHuntWish` latch in cohortWallet.ts (store.autoHunt is a display mirror of current config → the old heroConfigDiff was ALWAYS null; NO optimistic store flip — races syncFromEngine) + setBotSettings stripped in cohort drainInput (engine lane-0-only) + wantsBotTownTrip gated on MY hero's master · zone-unlock FULL per-member credit (`liveZoneKills` folds the live current-zone counter — engine stashes kills only on zone LEAVE; `settleProgressSlice` = base + max(0, sharedNow−sharedBase) per key union; settle-then-rebase at re-seed mirrors the wallet; HUD gauge shows my virtual kills via throwaway snapState override; unlockedZones display stays shared). (2) **World layer (relay f540750 + client abdad31, THE ONE RULE: presence/chat are render/store-only — zero code paths to pendingInput/engine state, pinned by a 2-client×800-turn garbage-feed hash-equality guard test):** relay additive presence rooms per zoneKey (pub/sub NO seq/slot/grace, last-value cache on pjoin, fan cap 12, payload 256B, v:1 versioned) + global chat single room (30-min in-memory ring buffer + 200 cap, injectable clock, text-only ≤120 chars, 1msg/2s SOFT c-rej never a kill, sender name from the HMAC ticket) + ping→pong point-to-point pre-join-allowed + `POST /api/presence/ticket` (guests allowed, displayName/classId/tier server-derived, ticket kind enforced BOTH ways vs party tickets); client world socket (`src/app/(game)/presence/worldSession.ts`) fully separate from the party socket, ghostStore (350ms x-lerp, 10s prune, cohort-member exclude — keys on displayName, party wire lacks charId = rare collision is a cosmetic double-draw, cap 12 + fps-EMA valve 12→6→0), publisher ~3Hz change-or-keepalive {v,cid,name,cls,tier,x,t} ~80B, render = pooled HeroView rigs via new `HeroRenderModel` pick-type in a `ghosts` container BETWEEN background/entities (under real heroes, structurally outside hitTestPointer), **walk/idle poses ONLY (owner cut attack/dash — no "hitting air")**, settings toggle ghostsVisible default ON. (3) **UX round (bcfaec4 + c004a24):** CohortStatus strip DELETED ("เล่นกับ xxx ในโซนนี้" read as debug) → 4-bar signal chip arena top-[14%] right-2 (RTT emerald<120/amber<300/rose, pulsing connecting/reconnecting, waiting=รอเพื่อน shows the laggy member's initial, hidden solo; tap popover = my relay RTT + per-member lane lag ms = lagTurns×100) fed by partySession ping/pong 5s + emaRtt + cohortTurnEngine `perSlotLag()`/public `bufferedAhead()` → cohortNet store field at ~1Hz · world-chat UI (💬 floating button + unread badge, ModalPortal bottom-sheet/360px column, 2s cooldown, c-rej→notice toast, 30-min prune-on-read; socket opens on ghostsVisible OR chatOpen, ALWAYS cjoins, pjoin stays ghost-gated) · DropFeed de-clutter (commons/stones → in-arena bottom-right coalesced stack cap 3 + "+N" overflow + stone qty merge-in-place 2.2s via pure dropFeedCoalesce.ts; epic keeps the top-center 5s discovery beat). (4) **mage apocalypse STEALTH field-wide buff (2a3061b — owner: NOT in patch notes):** apocalypseOffsets ±300→−650..+640 (gaps ≤230) + radius 150→200 = the 8 blasts tile the ENTIRE spawn band from any centroid; lone-boss hit count pinned at 3/8 by test (NOT a boss buff by design); sim 5400s GEAR+REFINE ×5 vs pre-buff baseline: all gates hold, s30 boss 4/5→5/5 (field coverage clears SUMMON adds — a baseline seed walled 93 wipes), hp potions 183→166/run, deaths flat. **DEPLOY this round: redeploy RELAY (required — presence/chat/ping) + web; NO db push. Owed: manual 2-tab ghost/chat playtest + mobile chip/chat-panel pass + owner playtest before merge (per-merge confirm rule).** Note: src/lab/pixelWeaponFx.ts + public/lab-assets/swd_010-012.png are the owner's own untracked WIP — leave them.

## Earlier (v6, party LIVE + big-patch sprint): **M8 party is LIVE in production and owner-stabilized through 4 live-test rounds (PRs #19–#24, each merged to main with per-merge owner confirm) + audio hotfix PR #25** — tests **1383/1383**, SAVE still v17 (ninja will bump to v18). Develop is AHEAD of main by the world-boss waves. **Live-test stabilization arc (all owner-reported, all fixed):** r1 connection (PR #19-20: relay /health CORS + no-cors best-effort pre-wake; late-joiner zone-beat deadlock — members re-announce beats on member-joined/-unshadowed; truthful cohort chip; my-hero-first store snapshot; auto-equip unknown-class conservatism = the 409 class_req spam) · r2 the big one (PR #21: **cohort FREEZE root-caused — first inputs stamp executeTurn=2 but nobody ever sends turns 0-1 → new `src/app/(game)/cohortTurnEngine.ts` pre-seeds idle lanes for turns 0..INPUT_DELAY-1**, issue/execute counters split, per-frame SUB_STEP_MS metering w/ stall-debt cap + soft catch-up = smooth 60fps not 100ms jumps; zero-loss input coalescing at turn edges; `buildFrameInput.ts` remaps castSkills/evolveHero/acceptQuest to myHeroIndex — non-leader skill taps used to command the LEADER's hero; real names via `resolveMemberDisplayName` never cuid; pov-gated spectacle `setPovHeroIndex` — friend ultimates no longer hijack your screen, world fx at the friend's position stay = intended co-op feel; SkillBar renders heroes[0] only; **party cap 3→6** relay MAX_SLOTS + MAX_PARTY_SIZE — relay redeployed by owner) · realtime friends (PR #22: poll 20s/60s → 5s open/15s closed + actionable toasts for party invites/friend requests — they felt dead until menu reopen) · r3 (PR #23: hidden tab no longer stalls the cohort — shadowed-slot lane auto-fill + visibilitychange hide→leave/show→rejoin; **any zone-change intent = collapseToSolo first** (`hasZoneChangeIntent`/`sanitizeLanes` over ZONE_CHANGE_KEYS; challengeBoss deliberately shared) so warp no longer drags the party; town walk iterates ALL heroes; relay clean-close 1000 = instant shadow; "4 shows 3" was a stale relay deploy) · r4 (PR #24: **per-member cohort wallets `cohortWallet.ts`** — stash WalletSlice at activation, virtual = base + trunc(sharedDrift/N), fixes gold/stones displaying swapped between players; rotating drop assignment FNV(rollId)%N ends N× drop duplication; mid-cohort bot toggles via heroConfig sync in drainInput; reload re-handshake + 10s handshake deadline w/ backoff = stuck-"connecting" fix; **multi-select sell** `ui/gear/multiSelect.ts`; party-panel copy now teaches walk-to-zone + warp-to-regroup). Same-day fixes: **warp-on-victory hang** (victory early-return in step() accepted fastTravel but never ticked it — now ticks the channel; same class as the town early-return bug) · **audio hotfix PR #25** (document-level pointerdown+keydown unlock + visibilitychange resume — no more tapping the canvas for sound; shipped via origin/main worktree, merged back into develop). **World boss "เสี่ยจ๋อง" CODE COMPLETE on develop (NOT yet on main — releases with ninja as ONE big patch):** hourly top-of-hour spawn in a deterministic-random map-1 farm zone (pure schedule helpers `worldBossWindowId/PhaseAt/ZoneFor` in engine, `CONFIG.worldBoss`), 5-min countdown banner that never reveals the zone (players hunt), 15-min lifetime, transient `GameState.worldBoss` (no SAVE bump, hash present-only), 0 xp/gold/quota from the boss itself; rewards per character per window via `WorldBossClaim` ledger (**prisma additive — needs the deploy-time db push**): 5,000 gold + 350 stones + a 50:50 แกร่งอาวุธ/แกร่งชุด **fortifier** (ItemInstance kind "fortifier", separate FORTIFIER_TEMPLATES catalog, guaranteed +1 refine at NORMAL gold+stone cost, cap +10, bot never sells, owner wants future central-marketplace trading); render = ~2.5× flashy-tycoon look distinct from every bossThemes entry (+3 latent shared-event-consumer bugs fixed: telegraph durations, BOSS_CY anchoring, resolveBossTint); balance verdict `docs/balance-worldboss.md` (hp 1.9M/atk 800: solo L90 sword+archer GATED, **mage leaks ~13min — owner accepted option ก**; 2p 12.5 / 3p 9.8 / 6p 2.8-4.6 min; rewards +19/+7/+2/+1% hourly by era = not inflationary). Known quirk to fix pre-release: boss lifetime pauses while a player travels (updateWorldBossAI skipped mid-travel). **NINJA CLASS CODE COMPLETE (same day, 5 waves, owner-approved shape):** 4th special class — unlock = account has ALL 3 base classes at tier 3 (server gate behind ONE removable flag `REQUIRE_NINJA_UNLOCK`), char slots 3→4 (4th slot ninja-only) + `Character.tier` cache column (stale-at-1 until each char's next save — accepted), DEX melee dual daggers (double-hit 2×0.55, range 70 shortest, cadence 0.45 fastest), dash primitive (`heroDashed` event → shadowDash fx), 4 skills เงาพริบ/คมเงาคู่/เงาสังหาร/พันเงานิรันดร์ + tier chain นินจา→จอมนินจา→ราชันเงา + SAVE v18 (pure widening); daggers t1-t10 ride the SHARED weapon curve (+10% comes through the double-hit) and are **class-gated drops — 3 legacy classes' loot byte-identical, daggers only roll with a ninja in the roster**; locked "4th card" create UX w/ tier-3 progress; sim verdict `docs/balance-ninja.md`: ALL GATES PASS (s1-30, both quest bosses, canonical 3-class hash unchanged) — the draft was too WEAK not too strong, shipped: hpMult 1.35, massacre 40 mana (≤50 ult rule), eternal 72/14/×9.0, ratio 4DEX:1VIT:1INT, boss DPS +6% vs sword (deliberately under the +10-15% target to avoid a strictly-better card). **Ninja LOUD FLAGS pending owner:** hpMult 1.35 vs the doc's thin-body 1.15 identity call · highest-friction class (562 deaths — same no-evasion root as archer; engine dash-to-evade wave would relieve both; playtest for feel) · eternal deviates hard from draft (was mechanically non-functional) · +6% DPS lever exists at dominance risk. **HOF seasonal rewards CODE COMPLETE (3 waves, `docs/hof-rewards-design.md`)**: monthly Thai-midnight cutoff via lazy-finalize (`HofSeason`/`HofAward` additive tables — **need their own db push at this patch's deploy**), 4 boards w/ rank-1..3 title ladders (boss-time cut from v1), titles derive from the latest finalized season (structural "board.rank" ids, Thai names i18n-only), rank-1 = champion gold aura (tall halo + orbiting motes, distinct from every other aura) + town honor plaque (x=150) + 50:50 fortifier claim (WB mint pipeline; online board = titles only), permanent profile badges, chosen title via uiConfig validated server-side, other-player title/champion rides the friends poll, Settings title picker + HOF ChampionsSection + claim CTA; known limit: profile badges resolve exactly only for the viewer's own row (legacy /api/hof carries no characterId). **Post-release same-day round:** hotfix PR #27 (world-boss zone lifecycle — town haunt + flee-burns-the-window, fixed w/ sweepWorldBossPresence in town/victory branches + only-defeat-burns-the-window; ninja daggers silently rejected at claim — classifyClaim now scans the superset table) · **FLAT shop pricing** (owner call: priceStageBase 1.12→1.0, everything ป้าปุ๊ sells = base price forever hp60/mana45/return150/warp200; sim-verified all gates hold, ninja deaths 562→449; OWNER-ACCEPTED DEBT: late-game gold accumulates faster — owner plans event sinks, revisit before central marketplace) · patch-notes 2026-07-09b announces the ENTIRE round per owner directive. **Owner-feedback round 2 (same day, PRs #29-30 merged):** HOF panel redesigned twice on live feedback — first the board-tab-driven PodiumStrip + skeleton/fixed-height/session-cache tab stability (#29, "ui ไม่ค่อยได้เรื่อง"+"ยึดๆหดๆ"; also fixed the shrink-0-truncate title bug that squeezed names), then the collapsed strip felt "ไม่พิเศษ" → **3-slot podium STAGE 2|1|3 always visible** (crown + gold breathing glow champion card, engraved placeholders keep heights fixed, mobile reflow) + density pass (sm:max-w-2xl desktop, single rank marker, fixed value columns) (#30). **Ninja flags all owner-decided:** hpMult 1.35 stays · eternal stays · cadence ×1.25 faster at EXACT DPS-neutral (atkSpeed 0.36 / multiHitMult 0.44, boss edge ~+4% after rounding — deliberately uncompensated) · **dash-evade bot behavior** (swarmed AUTO ninja blinks out via CONFIG.ninja.evade, HeroType.dashEvade capability flag, transient hash-excluded counters, deaths 449→368 −18%, all gates hold, 3-class md5 identical) — the archer still lacks evasion (no dash primitive; future call). Patch-notes 2026-07-09c/d announce everything. **ENDGAME v1 SHIPPED (same day, PRs #33-35 merged, owner-confirmed each):** buff hub v2 source-labels (#33) then arena-OVERLAY icons at top-[14%] (never shifts layout, clears the boss bar); **ดินแดนอสูร** map 7 (10 zones s31-40 post-s30, soft +8/+9/+10 refine bands per owner pick ข NO hard door, deterministic ELITE + asuraEssence/zone-stone accrual, daily hot zone FNV x1.4, SAVE v19, docs/balance-asura.md — flags: mage trivializes deep/ninja walls z8, refine can't hard-gate via mults); **secret tome quest → legendary craft** (3 burnt pages: first-elite + z5/z8 depth milestones → smith '!' badge + lore dialogs → '⚒️ ตำราตำนาน' menu, SAVE v20; craft = sacrifice t10 + essence12/sila 10x80/daily sigil (AsuraSigilClaim table, **needs db push**) + 50k gold/200 stones → bind-on-craft LEGENDARY_TEMPLATES per class at t10+10 PARITY (mult 1.8), never sells/drops/breaks, first-craft announce; **awakening +0..+5** at LEGENDARY_AWAKEN_STEP 16%/step = +5 ≈ **+80% over t10+10** (owner escalated 40→80), costs 100k→1.5M gold ladder (~3.25M total sink), guaranteed no-break, per-level fx escalation to the +5 'จุติ' glow; patch notes = owner-approved teaser copy, ZERO legendary spoilers — discovery is fully in-game); ninja unlock OPENED (flag false); party reconnect crushed (beat-before-offer bug = 10s stall every town trip → <1s, prewake skipped on first connect, chip 600ms grace); cohort title fix; inventory paper-doll overflow fix + per-class weapon glyphs + equipped-first sort; friends sorted online-first. Deploy done by owner except: **db push AsuraSigilClaim + web redeploy pending at merge time.** **Live-feedback round 3 (PR #36, 13 items, owner-confirmed):** world boss SHARED HP pool server-wide (WorldBossWindow/WorldBossDamage additive tables — atomic floored decrement, participation-gated claim, cohort damage dedup = only lowest-slot posts + participation ping, GET state seeds spawn hp; potion-trip reset gone by construction) + bots pile on an engaged world boss solo too (never initiate); cohort PROGRESSION leak plugged (cohortProgress.ts stash mirrors cohortWallet — partying no longer permanently unlocks the leader's zones) + per-member bot toggle (buildSnapshot read the shared lane-0 autoHunt) + title visible to PEERS (persistSave blind-overwrote uiConfig killing displayTitle — now read-merge-write) + cohort re-form keeps positions (optional x on ReseedOffer) + tap-rings pov-gated (heroIdx on moveOrdered/targetLocked) + party invite never-silent (respondPartyInvite discarded error codes; partyErrorKey toasts + inParty chip); refine REVEAL-ON-FINAL-STRIKE UX (executeRefine mutated the store on POST resolve = the leak; split applyRefineResult committed only at the reveal frame; 3-4 beat suspense, amber 0.45 flash for dark rooms); RENAME feature account+character once/Bangkok-day (renameDay CAS columns). Suite 1826/1826. DEPLOY: db push (WorldBossWindow, WorldBossDamage, renameDay x2 — additive) + web redeploy. **Big-patch deploy checklist (READY — awaiting owner merge confirm):** owner confirms merge → `prisma db push` (WorldBossClaim + Character.tier, both additive) → redeploy web only (relay already current at cap 6). **Owner owes:** merge confirm · ninja flags above · endgame-direction answers (3 questions re post-s30) · 3 old cohort balance flags (auto-hunt clustering, boss-melt-at-headcount, buff shape). Known open quirks: channelled-travel leave shows peers "waiting" until the arrival beat (relay "leave" opcode would fix); cohort re-seed resets world-boss HP (accepted v1).

## Earlier (v5): **M1–M7.95 complete + M8 "Party & Friends" core MERGED TO MAIN (PR #18, owner-confirmed)** — SAVE now **v17**. ⚠️ **deploy blocker: owner must run ONE `prisma db push`** (User auth cols + FriendRequest/Friendship/EmojiPing/PartyInvite/Party/PartyMember/DailyClaim, all additive) or auth/friends/party/daily endpoints 500 — game core unaffected. **M8 CODE COMPLETE on develop (same day):** infra = **Render free tier Singapore** (owner deployed the probe: RTT p95 42ms, 0 drops; Hostinger confirmed outgoing-only in writing) · P4a zero-dep relay `scripts/party-relay/` (single seq stream for game+control = total order, HMAC tickets via /api/party/ticket w/ slot from PartyMember.joinedAt, 5s grace → member-shadowed, /health pre-wake for Render sleep) · P4b client (partySession/partyHandshake in src/app/(game): zone-beat cohorts, seed authority = lowest slot, cohort state = authority's shared economy slice + each owner's hero (per-client slices would hash-diverge), turn 0 starts when reseed-acks complete — same seq position everywhere, seq-gap = fatal rejoin, cohort autosave = toSaveData({...state, heroes:[mine]}), HUD CohortStatus chip, docs/party-dev-setup.md + pnpm relay) · P6 render (all heroes full-rig w/ nameplates via setHeroDisplayNames seam, state-driven shadow dim+offline tag, join/leave juice; solo output pixel-identical) · cohort balance (share 0.6, buff ×1.04/1.08, spawn ×1.5/2.0 → xp/hr per member ×1.25-1.46; solo byte-identical; LOUD FLAGS pending owner: auto-hunt target-clustering starves cohort kills 45-65% of solo — needs engine fix wave; bosses MELT at headcount — HP scaling is owner's call; flat-per-cohort buff shape if bigger bonus wanted). Tests **1258/1258**. **Owner checklist to go live: ONE prisma db push · create 2nd Render service (root scripts/party-relay, env PARTY_RELAY_SECRET) + set PARTY_RELAY_URL/SECRET on the game host · 3-tab manual test per docs/party-dev-setup.md · finish probe broadcast/10-min checks · calls pending: tier-4→M8.5, 3 balance flags, P5 replay validation deferred to follow-up.** **M8 landed:** account system (/welcome Login/Register/**Guest**; email+password NO-verify unique-email-only per owner; guest upgrades IN PLACE at Settings→My Account; scrypt, friendCode/displayName per account; identity swap lived entirely behind `src/server/identity.ts` as designed) · friends MVP (single 20s/60s/hidden-paused poll: presence online-<300s/current-character/zone via `Character.lastZone` stamp — never parses the blob; emoji 12 Win10-safe glyphs 10/min; requests by code or char-name, reverse-pending auto-accepts; guests 403 `account_required`) · party social-container v1 (Party/PartyMember `userId @unique` = 1-party-per-user DB-enforced, cap-3 re-checked in tx under row-lock, leader-leave promotes oldest, invites via friend panel; party state rides the SAME friends poll) · **lockstep engine 100%** — P1a `dmath` (LUT sin/int-pow; Math-transcendental source-scan guard; rebaseline was FREE — sims byte-identical), P1b multi-hero (`step(state, FrameInput|PartyInput)` lanes, per-hero `HeroConfig` w/ single-writer `applyHeroConfig`, solo path byte-identical), P2 shadow-body (transient `shadowed` + lane-collapse policy; income = normal offline-idle แบบ ก), P3 vitest lockstep harness (2-3 clients × thousands of turns hash-equal; lanes apply on sub-step 0 ONLY — matching GameClient; 1-ULP canary trips) — **P4 relay/P5 replay/P6 render BLOCKED on infra: ws spike verdict = Hostinger shared hosting forbids incoming websockets (official doc); zero-dep probe `scripts/ws-probe/` + Thai README awaiting owner's real-host run → VPS call** · quest system full (`docs/quest-design-m8.md`; main chapters WRAP the derived goal-ladder — only `mainClaimed` persists, migration marks-done-NO-backpay; dailies 3/day presence-style, rewards gold/stones/potions NEVER power, serverDay Asia/Bangkok + `DailyClaim` unique ledger, roster piggybacks /api/save, claim = POST-first then engine intent; quest board at NEW third town NPC **ผู้ใหญ่บ้าน** x=400 w/ claimable-! badge, town-only, zero bot hooks; **tier-4 class proposed → M8.5, awaiting owner**) · friend-warp scroll 🌀 (pahpu base-200; party-panel warp-to-member; target zone must be SELF-climbed — warp never grants access; bot never warps) · **owner interjections shipped same-day**: mana relief (tier3PoolBonus 90→170, skyfall 120→80, storm 90→45 → sword −48%/archer −47% potions, mage flat, ALL gates held) · enhancement stones REPLACE salvage (stone roll rides gear rolls on a domain-tagged stream — gear sequence byte-identical, NO save bump; claim = same idempotent pipeline w/ born-dead receipt rows; salvage endpoint 410-stubbed, bot sell-only, stockpiles intact) · `/lab` experiment page (registry of 6: anim/inBiome/sideBySide/juice/playground/townPreview; uploads persist to `public/lab-assets` dev-only-writes; frozen-animation bug fixed via virtual all-frames group) · town llama decor from the owner's OWN pixel art (tappable hop+hearts; graceful no-op sans files) · sprite sandbox `scripts/sprite-sandbox/`. Tests **1204/1204**, canonical sim gates all hold, s1-15 byte-identical maintained through every wave. **Owner owes: ONE `prisma db push`** (User auth cols + FriendRequest/Friendship/EmojiPing/PartyInvite/Party/PartyMember/DailyClaim), ws-probe run, tier-4→M8.5 call + 4 minor quest flags (defaults live), merge-to-main confirm. Earlier: (SAVE v16 era; DB fully synced — owner ran `prisma db push` 2026-07-08 after PR #16, `SoldItem` verified live via information_schema). Round-4 (owner live-playtest, same day): **town manual-play fix** (PR #14 — `step()`'s town early-return skipped `applyManualCommand`+`updateHeroes`, so tap-to-move was dead in town and the phase-3 NPC talk range was unreachable; new walk-only `tickTownManualWalk` in the town branch, botWalk keeps priority, fast-travel channel stands still; +5 regression tests that run through `step()` IN TOWN — the path the M7.8 suite never exercised) · **ป้าปุ๊ shop = 3 tabs** (PR #15–16: [ซื้อของ|ขาย·ย่อย|ซื้อคืน]) — sell/salvage tab reuses the inventory flows exactly (shared `ui/gear/sortRank.ts` + `useConfirmGuard` extracted; InventoryPanel behavior unchanged; hammerable, stays open per action) · **NPC buy-back** (PR #16 — additive `SoldItem` table, the sell endpoint records rows in the same tx; GET/POST `/api/items/buyback`: server-stamped 3-day window (`BUYBACK_WINDOW_DAYS`), atomic check-and-set restore re-mints the instance w/ refineLevel preserved + `origin:"buyback"` (drop-rate ceiling untouched) + `boughtBack` ItemEvent; salvaged items excluded by design (materials already granted — dupe risk); manual-only, no bot wiring; gold check reads the persisted save blob = the SAME refine MVP gap, not a new one) · patch-notes releases 2026-07-08c (town fix + shop sell) and 2026-07-08d (buy-back). Bot epic-dispose: owner asked for it — **already shipped** (v3 "option A": settings→Drops→ของตำนาน 3-way action, keep-guard forced, equipped items double-protected); no change made. Tests 1009/1009; engine change nav-only + server additive — no sim adjudication needed. In-browser pass of the new shop tabs (desktop+mobile) still owed. Round-3 highlights: **town NPCs full loop** (ป้าปุ๊ merchant / ลุงดึ๋ง smith — tap-again-to-talk, shop/refine panels ONLY via NPC talk via TownNpcPanelHost, bot walks to pahpu ~3.5s/trip, anchors = CONFIG.townNpcs) · **easy fast-travel** (channel starts mid-combat, damage never interrupts, death-only cancel; boss phase still locked) · **quest integrity rules (owner)**: climb-first tundra gate (`tier3GateCleared` — map3 boss room must be persist-unlocked; boot strand-guard), quest-leads routing (`botFarmTarget` — all auto-routes honor the quest zone), auto-advance ONLY on fresh unlock (deliberate old-zone farming respected), quest card outranks boss-door on the goal ladder, quest-boss softlock fixed (`returnToQuestFrontier`) · **in-session update banner** (build id rides /api/save, flush-then-reload) · patch-notes 2026-07-08b. Tests 984/984; canonical sim (5400s GEAR+REFINE) all gates hold, quest boss won 3/3 classes. NOTE for sim runs: default SIM_SECONDS=1800 without GEAR CANNOT beat the tier-3 quest boss (false alarm history — always adjudicate with the canonical config). Next: M8 Party (VPS/websocket spike first). **M7.95 Hall of Fame + UAT round-2 polish CLOSED + merged (PR #12)** — HOF 5 boards (level/power/goldEarned/boss-times/online) + class filter + profiles + anti-cheat re-derive (M5 debt closed; suspect=hidden not banned) + first-to-90/rank-1 announcements + deleted-char purge; tier-3 quest reborn (map4-z1 tundra preview grant → 90 kills → quest-scaled young Glacial Sovereign → evolve → s15); bot single-switch + consolidated settings + per-character `Character.uiConfig` persistence; 12 mob species maps 4-6 (enemySpecies.ts); aim-driven hero facing (Hero.aimX transient); war-cry buffs ALL allies (party-ready) + aura/chip; UX-fix wave ×10 (quest actions live on the quest card now — SkillBar affordance deleted; skill ⓘ inspectors via skillStats.ts); warp menu themed; inventory unstacked best-first; refine tap-to-skip; wave remnants purged (waves.ts→hunt.ts, state.wave/waveGap gone). Owner cadence rules: NEVER merge to main without per-merge confirm; automation must stay dumb; /proto-shaders rejected+removed. DB schema fully in sync as of 2026-07-08 (owner ran `prisma db push` after PR #12 — announcements schema applied; the P2022 `kind` error is resolved). Next queued: town NPCs (plan approved, awaiting arrival-behavior pick) → M8 Party (VPS/websocket spike first). Earlier: **M7.9 Grand Expansion merged (PR #10-11)** (SAVE v15, world ×2 maps 4-6, class tier 3, gear t7-10, boss variety, ModalPortal rule for all modals). **M7.9 "Grand Expansion" CLOSED + merged to main (PR #10)** — world ×2 (maps 4-6: ice tundra s16-20 / desert ruins s21-25 / hell city s26-30, levelCap 90, s1-15 byte-identical), class tier 3 @Lv40 quest (จอมอัศวิน/ราชันพราน/อาร์คเมจ, SAVE v15 domain-widening, skill-4 `sword_skyfall`/`archer_storm`/`mage_apocalypse` + spectacles incl. 0.16s time-freeze via timeDirector, auto slot 4 tier-gated), gear t7-t10 (46 templates, refine ceiling t10+10 = 126 atk), 6 unique boss looks (`bossThemes.ts`) + boss mechanics s20 CHARGE / s25 SUMMON / s30 FIELD HAZARD with full telegraph fx/sfx (5 new events), codex/FTUE/i18n complete, patch-notes release 2026-07-07c. Same-day owner asks: fast-travel picker closes on select; **mobile modal stacking fix — iOS Safari treats ancestor backdrop-filter as a containing block for fixed children, ALL modals now render through `ModalPortal` (new modals MUST too)**. Balance = `docs/balance-m79.md` (all 6 gates hold; sim-harness now routes the tier-3 quest backtrack + BOSSISO mode). Open flags: archer s26-30 friction high (class-design follow-up), in-browser visual pass of all M7.9 render work still recommended (boss horns vs HP bar, skill-4 spectacles). Tests 736/736. Earlier same-date history: M7.7 "Skill Spectacle & World Heat" (SAVE v13). M7.7 landed: 3-layer skill rework (signatures bigger, utilities keep roles, tier-2 = field-wide ultimates r460/13-drop; NO new ProjectileKind), mana = pacing governor (regenPerIntPoint 0.05; potions/run sword 124 / archer 181 / mage 17 — a real sink), survivor-retaliation replaces the aoeWake caps (skill-damaged survivor engages), denser fields 17/19/21 + killGoal ×1.5 with xp/gold ÷1.5 (per-zone totals byte-identical), per-skill render language + ultimate spectacle (skyDarken/groundCrack/curtainSweep, shake 15-17), mana bar promoted in HUD. Same-day fixes: class bug (every new char booted swordsman — repairHeroClass + boot fallbackClass, 1d5ba4d), zone-unlock gauge persistence (SAVE v13 `zoneKills` — town trips no longer wipe it), bossReady arms ONLY at the boss-gate zone (was quota-anywhere → dead glowing button; test/sim autopilots now advance on next-zone UNLOCK), wave-era copy sweep (boss-door language, FTUE bossChallenge step re-taught), bot-status toasts, context-aware goal card (town/free-farm hints, power% display-capped 999). **M7.7 CLOSED (2026-07-07)** — auto-allocate v2 landed: per-class ratio distributor ("next point to the stat farthest below `stats[s]/weight[s]`", self-correcting, no persisted counter, no SAVE bump); sim overruled the draft → **sword 4STR:1VIT, archer PURE DEX** (any VIT share regressed — DPS-race class), **mage 3INT:1VIT**; gates held (class change s5, s15 wall intact, 0 stalls; deaths sword 183→24, mage 50→20) — details in balance-m7.md "Auto-allocate v2". PR #3 (M6+M7) and PR #4 (M7.6+M7.7+M7.8) merged to main; **PR #5 (UAT batch: playtest fixes + prestige/announcements/stat-tap/patch-notes/catch-up) open** awaiting owner merge. **M7.6 Refine ตีบวก CLOSED (2026-07-07)** — 4 waves: engine (SAVE v14, `refinedStat = base×(1+N×8%)`, materials counter, engine never rolls) → server (additive refineLevel/materials columns db-pushed, salvage/refine endpoints, atomic check-and-set + crypto roll, ItemEvent refined/salvaged) → sim (draft rates CONFIRMED unchanged: s15 wall 0/15 even refine-stressed, materials a real sink, +10 ≈ 359 attempts, break ≈ 1% of drops; REFINE=1/sweep/STRESS knobs) → UI (town refine station w/ hammer-strike juice, bulk salvage + yield preview, compound templateId:refineLevel stacking, materials HUD readout, +N names, aura step-up at +7; goldCredit intent now signed). Known MVP gaps: gold check reads persisted save-blob (save-before-refine, client-authoritative gold), town-only client-enforced like sell, final in-browser visual pass of the refine panel unverified. **UAT polish batch CLOSED (2026-07-07, post-playtest)** — bot dispose hardening (auto-equip failure no longer silently skips sell/salvage; owner-confirmed fixed), ranged kite jitter one-line fix (target-relative servo + regression test), INT share sword 4:1:1 / archer 4:1 (potion burn −55%; balance-m7.md "Owner INT pass"), +8/+9/+10 prestige aura ladder (render-only, pooled, distinct from t6), UAT patch-notes modal (once per release id, skips brand-new players), tab-return catch-up replay (>5s hidden → boot offline path, capped/budgeted). ROADMAP "UAT polish batch" has the list. **M7.8 Manual Play CLOSED (2026-07-07)** — engine: moveTo/attackTarget/cancelCommand FrameInput intents (Hero.command transient, no SAVE bump; boss forced-combat wins; auto path byte-identical; paves M8 lockstep) · UI/render: hitTestPointer via baseTransform (shake-safe), tap ground→move w/ ring ping, tap monster→persistent lock reticle, cancel chip beside AUTO, first-AUTO-off tip th/en, mouse+touch equal. Manual skill-cast buttons deferred to backlog (overlaps mana governor). In-browser device pass unverified — recommend owner playtest. Next: **M8 Party** (FIRST: VPS/websocket infra spike decision). **M6.5b UI Skin: owner REJECTED the warm-fantasy title (3rd art attempt struck down) — reverted (4c856df), shelved again; next attempt must start from owner-provided visual references, not from building.** **Owner directive 2026-07-06: every UI/interaction must play comfortably on BOTH desktop and mobile** (touch-first, responsive HUD). → M8 Party (FIRST: VPS/websocket spike decision) → M9. PvP cut; prestige on hold. Balance baselines = `docs/balance-m6.md` (world) + `docs/balance-m7.md` (gear/vendor + M7.7 section).
